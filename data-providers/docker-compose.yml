services:
  rabbitmq:
    image: rabbitmq:3.13-management
    container_name: szlakomat-rabbitmq
    ports:
      - "5672:5672"
      - "15672:15672"
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    healthcheck:
      test: ["CMD", "rabbitmq-diagnostics", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  pricing:
    build:
      context: .
      dockerfile: src/DataProviders.PricingProvider/Dockerfile
    container_name: szlakomat-pricing
    environment:
      - RABBITMQ_HOST=rabbitmq
      - MOCK_DATA_PATH=/app/mock-data/attractions.json
    volumes:
      - ./mock-data:/app/mock-data:ro
    depends_on:
      rabbitmq:
        condition: service_healthy
    restart: unless-stopped

  events:
    build:
      context: .
      dockerfile: src/DataProviders.EventsProvider/Dockerfile
    container_name: szlakomat-events
    environment:
      - RABBITMQ_HOST=rabbitmq
      - MOCK_DATA_PATH=/app/mock-data/attractions.json
    volumes:
      - ./mock-data:/app/mock-data:ro
    depends_on:
      rabbitmq:
        condition: service_healthy
    restart: unless-stopped
